# Template for a real dataset. Copy this file, rename it, and point
# `dataset_dir` at a folder containing train.jsonl (+ optional
# validation.jsonl / test.jsonl) in the chat schema documented in README.md.
name: my-dataset
source: local
dataset_dir: data/my-dataset
max_seq_length: 1024
max_train_samples: null
default_system_prompt: "You are a helpful assistant."
